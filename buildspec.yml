version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "Installing dependencies..."
      - pip install -r requirements.txt -t lib
      - cd Newlambda
      - pip install -r requirements.txt -t  lib
  build:
    commands:
      - cd ..
      - echo "Zipping deployment package..."
      - cd lib
      - zip -r9 ../deployment_package.zip .
      - cd ..
      - ls
      # - zip -g deployment_package.zip lambda_function1.py
      # - zip -r9 ../deployment_package.zip lambda_function.py
      # - cd ..
      # - zip -g Newlambda/deployment_package.zip lambda_function.py
  post_build:
    commands:
      # - echo "Updating lambda Function..."
      # - aws lambda update-function-code --function-name lambdaCICD1 --zip-file fileb://Newlambda/deployment_package.zip
      # # - aws lambda update-function-code --function-name lambdaCICD1 --zip-file fileb://deployment_package1.zip
      # - echo "DONE update"
      # - aws lambda list-functions